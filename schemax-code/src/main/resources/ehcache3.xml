<?xml version="1.0" encoding="UTF-8"?>
<config
        xmlns:xsi='http://www.w3.org/2001/XMLSchema-instance'
        xmlns='http://www.ehcache.org/v3'
        xsi:schemaLocation="http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd">

    <!-- 持久化目录配置 -->
    <persistence directory="config/ehcache/data"/>

    <!-- 默认缓存模板 -->
    <cache-template name="default">
        <key-type>java.lang.String</key-type>
        <value-type>java.lang.String</value-type>
        <expiry>
            <tti unit="seconds">120</tti>
        </expiry>
        <resources>
            <heap unit="entries">10000</heap>
        </resources>
    </cache-template>

    <!-- info 缓存配置 -->
    <cache alias="info" uses-template="default">
        <key-type>java.lang.String</key-type>
        <value-type>java.util.List</value-type>
        <expiry>
            <none/>
        </expiry>
    </cache>

    <!-- data-loading 缓存配置 -->
    <cache alias="data-loading" uses-template="default">
        <expiry>
            <none/>
        </expiry>
        <resources>
            <disk unit="MB" persistent="true">10</disk>
        </resources>
    </cache>

    <!-- data-metainfo 缓存配置 -->
    <cache alias="data-metainfo" uses-template="default">
        <expiry>
            <none/>
        </expiry>
        <resources>
            <disk unit="MB" persistent="true">100</disk>
        </resources>
    </cache>

</config>
